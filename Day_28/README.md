## Day 28 - Advanced CSV Import Options

Today I learned and practiced:

- Using the `header` parameter
- Using the `usecols` parameter
- Using the `nrows` parameter
- Using the `skiprows` parameter
- Using the `encoding` parameter

These concepts helped me gain better control over importing and preprocessing datasets using Pandas.
